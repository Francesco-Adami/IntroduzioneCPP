#pragma once

#include "Operatori.h"

class Calcolatrice
{
public:
	// strutture di controllo
	void EsempioIfElse(int valore);
	void EsempioSwitch(GiorniSettimana operatore);
	void EsempioFor(int limite);
	void EsempioWhile(int limite);
	void EsempioDoWhile(int limite);
	void EsempioForeach();
};